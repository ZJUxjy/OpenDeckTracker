## 1. Native counters and structured log line

- [x] 1.1 In `packages/hearthmirror/native/src/reflection/collection.rs`, refactor `get_collection_internal` to track six counters (`list_size`, `parsed`, `non_zero_dbfid`, `null_ptrs`, `field_misses`) and one optional `sample_class` while iterating. The per-element loop MUST increment `null_ptrs` when `read_remote_ptr` returns NULL, increment `parsed` when `child_from_address` returns `Some`, capture the class name of the first parsed element into `sample_class` (via the existing `mono::class::read_mono_class` or equivalent), and increment `field_misses` once for each of the three `read_int32_field` calls that returns `Ok(None)`.
- [x] 1.2 Wrap the function body in a `let start = std::time::Instant::now();` / `start.elapsed()` measurement and emit a single `eprintln!("[hearthmirror:collection] list_size={} parsed={} non_zero_dbfid={} null_ptrs={} field_misses={} sample_class={} elapsed={}ms", ...)` line at the very end (including on the early-return path when the singleton or `m_collectibleCards` pointer is null — in that case all counters are 0 and `sample_class` is the literal `<unset>`).
- [x] 1.3 Run `cd packages/hearthmirror/native && cargo clippy --release -- -D warnings -D clippy::unwrap_used -D clippy::expect_used -D clippy::panic`; expect exit 0.
- [x] 1.4 Commit with `git add packages/hearthmirror/native/src/reflection/collection.rs && git commit -m "feat(hearthmirror-native): instrument get_collection_internal with diagnostic counters"`; expected output includes a new commit hash.

## 2. CollectionDiagnostic struct and napi export

- [x] 2.1 Still in `reflection/collection.rs`, define `#[napi(object)] pub struct CollectionDiagnostic { pub list_size: i32, pub parsed: i32, pub non_zero_dbfid: i32, pub null_ptrs: i32, pub field_misses: i32, pub sample_class: Option<String>, pub elapsed_ms: i32 }`.
- [x] 2.2 Extract the counter-tracking core from `get_collection_internal` into a private helper `read_collection_with_counters(runtime) -> Result<(Option<Vec<CardResult>>, CollectionDiagnostic), ScryError>` so both `get_collection_internal` and the new diagnostic export can share the same walk. `get_collection_internal` MUST continue to return `Result<Option<Vec<CardResult>>, ScryError>` with unchanged semantics (it discards the diagnostic but still emits the eprintln from §1).
- [x] 2.3 Add `pub async fn get_collection_diagnostic_internal(runtime) -> Result<CollectionDiagnostic, ScryError>` that calls the shared helper and returns just the diagnostic (the cards Vec is dropped).
- [x] 2.4 In `packages/hearthmirror/native/src/lib.rs`, add a `#[napi] pub async fn get_collection_diagnostic() -> napi::Result<CollectionDiagnostic>` exported function using `with_runtime_or` (with a default `CollectionDiagnostic { all zero fields, sample_class: None, elapsed_ms: 0 }`) so unavailable-runtime resolves to a zero-counter diagnostic rather than null.
- [x] 2.5 Run `cd packages/hearthmirror/native && cargo build --release`; expect exit 0. If the previous `hearthmirror-native.win32-x64-msvc.node` is held by a running Electron dev session, stop dev first then rebuild.
- [x] 2.6 Verify `packages/hearthmirror/native/index.d.ts` was regenerated by the napi build and now exports `function getCollectionDiagnostic(): Promise<CollectionDiagnostic>` and `interface CollectionDiagnostic { ... }`. If napi build does not regenerate `index.d.ts`, run `pnpm --filter @hdt/hearthmirror-native build` from the repo root to invoke the napi-rs CLI explicitly.
- [x] 2.7 Commit native changes with `git add packages/hearthmirror/native/src/reflection/collection.rs packages/hearthmirror/native/src/lib.rs packages/hearthmirror/native/index.d.ts packages/hearthmirror/native/hearthmirror-native.win32-x64-msvc.node && git commit -m "feat(hearthmirror-native): add get_collection_diagnostic napi export"`; expected output includes a new commit hash.

## 3. TS wrapper

- [x] 3.1 In `packages/hearthmirror/src/types.ts`, export `interface CollectionDiagnostic { listSize: number; parsed: number; nonZeroDbfid: number; nullPtrs: number; fieldMisses: number; sampleClass: string | null; elapsedMs: number; }`.
- [x] 3.2 In `packages/hearthmirror/src/hearthmirror.ts`, add `async getCollectionDiagnostic(): Promise<CollectionDiagnostic | null>` that calls `native.getCollectionDiagnostic()` and either returns the result or `null` if the native call resolves to a falsy value. Re-export `CollectionDiagnostic` from `index.ts`.
- [x] 3.3 In `packages/hearthmirror/src/hearthmirror.test.ts`, add a test `getCollectionDiagnostic forwards the native counters` that mocks the native module's `getCollectionDiagnostic` to return `{ listSize: 100, parsed: 100, nonZeroDbfid: 95, nullPtrs: 0, fieldMisses: 5, sampleClass: 'CollectibleCard', elapsedMs: 1234 }` and asserts the wrapper returns the same object shape. Run `pnpm --filter @hdt/hearthmirror exec vitest run src/hearthmirror.test.ts -t "getCollectionDiagnostic"` and expect it to pass.
- [x] 3.4 Commit wrapper changes with `git add packages/hearthmirror/src/types.ts packages/hearthmirror/src/hearthmirror.ts packages/hearthmirror/src/index.ts packages/hearthmirror/src/hearthmirror.test.ts && git commit -m "feat(hearthmirror): expose getCollectionDiagnostic on TS wrapper"`; expected output includes a new commit hash.

## 4. IPC + preload + renderer

- [x] 4.1 In `apps/desktop/src/main/ipc.ts`, register an `ipcMain.handle('hearthmirror:get-collection-diagnostic', () => hm().getCollectionDiagnostic())` next to the existing `hearthmirror:getCollection` handler.
- [x] 4.2 In `apps/desktop/src/preload/index.ts`, add `getCollectionDiagnostic: (): Promise<CollectionDiagnostic | null> => ipcRenderer.invoke('hearthmirror:get-collection-diagnostic')` under the existing `hearthmirror` namespace. Import the type from `@hdt/hearthmirror`.
- [x] 4.3 In `apps/desktop/src/renderer/tests/setup.ts`, add a default stub for `hearthmirror.getCollectionDiagnostic` returning `null` so existing renderer tests don't break.
- [x] 4.4 In `apps/desktop/src/renderer/src/components/Collection.tsx`, extend `handleSyncClick`: include `window.hdt.hearthmirror.getCollectionDiagnostic()` as a fourth item in the existing `Promise.allSettled`, and after settling emit `console.log('[hearthmirror:collection]', diagnosticResult.status, diagnosticResult.status === 'fulfilled' ? diagnosticResult.value : diagnosticResult.reason)`. The button's terminal-state logic (driven by `progressResult.status`) MUST be unchanged.
- [x] 4.5 Run `pnpm --filter @hdt/desktop typecheck`; expect exit 0.
- [x] 4.6 Run `pnpm --filter @hdt/desktop exec vitest run src/renderer/tests/Collection.progress.test.tsx`; expect exit 0 (the existing tests don't assert on `[hearthmirror:collection]`, so the new console.log is silent for them; the existing button-state assertions still pass because the diagnostic call's outcome doesn't gate state).
- [x] 4.7 Commit IPC/preload/renderer changes with `git add apps/desktop/src/main/ipc.ts apps/desktop/src/preload/index.ts apps/desktop/src/renderer/tests/setup.ts apps/desktop/src/renderer/src/components/Collection.tsx && git commit -m "feat(desktop): pipe collection diagnostic into sync button console output"`; expected output includes a new commit hash.

## 5. Final verification

- [x] 5.1 Run `pnpm --filter @hdt/desktop typecheck`; expect exit 0.
- [x] 5.2 Run `pnpm --filter @hdt/hearthmirror typecheck`; expect exit 0.
- [x] 5.3 Run `pnpm --filter @hdt/desktop exec vitest run src/renderer/tests/CollectionSyncButton.test.tsx src/renderer/tests/Collection.progress.test.tsx`; expect exit 0.
- [x] 5.4 Run `pnpm --filter @hdt/hearthmirror exec vitest run`; expect exit 0.
- [x] 5.5 Run `openspec status --change diagnose-hearthmirror-collection-read`; expected output shows all artifacts complete and ready for archive.
- [x] 5.6 Manual verification (after restarting `pnpm dev`): open the Collection page in HDT.js with Hearthstone running, click the sync button, capture the `[hearthmirror:collection]` line from the renderer DevTools console **and** the same line from the main-process stderr terminal. Both lines MUST report the same counter values within the same call window.
