# 下一个候选 change

> 本文件按优先级列出下一个值得提案的 change。在动手前先用 `openspec new change <name>` 创建并完成 proposal/design/specs/tasks 流程。
> ✓ 表示已提案完成（artifact 全部 done），✓✓ 表示已实施 + 归档完毕。

## ✓ 1. `decide-hearthmirror-bridge`（已完成）

**类型**：纯决策 change（ADR），不写业务代码。

**结果**：ADR 0001 选定**方案 D（64 位 napi-rs，同进程）**，推翻了 `Rewrite_Design.md` 中"必须 32 位"的旧假设（64 位进程可以直接用 `ReadProcessMemory` 读 32 位炉石内存，把对端指针当 `u32` 即可）。

参考：
- ADR：[`docs/adr/0001-hearthmirror-bridge.md`](../../docs/adr/0001-hearthmirror-bridge.md)
- Spike 计划：[`docs/spikes/0001-hearthmirror-spike.md`](../../docs/spikes/0001-hearthmirror-spike.md)
- 详细评估：[`openspec/changes/decide-hearthmirror-bridge/design.md`](decide-hearthmirror-bridge/design.md)

> **下一步**：执行 `docs/spikes/0001-hearthmirror-spike.md` 计划的 spike，建议作为 `add-hearthmirror-bridge-spike` change（见下条）。

## ✓ 2. `add-hearthmirror-bridge-spike`（已完成）

**结果**：✅ PASSED。两个核心断言成立：

- napi-rs 3.x + windows crate 0.58 在 Electron 33 + Node 22 下零摩擦构建/加载（`.node` 模块 475 KB）。
- 64 位 Rust 用标准 `ReadProcessMemory` 跨架构读到 32 位 Hearthstone 模块基址前 16 字节 `4D 5A 90 00 ...`，普通用户权限即可，单次调用 ~252 µs。
- 关键观察：**ASLR 启用**，每次基址不同（实测 `0x002E0000`），后续实现禁止 hardcode 任何地址。

参考：[`docs/spikes/0001-hearthmirror-spike-report.md`](../../docs/spikes/0001-hearthmirror-spike-report.md)、[`docs/adr/0001-hearthmirror-bridge.md`](../../docs/adr/0001-hearthmirror-bridge.md)（已升级为 Validated）。

> **下一步**：进入 `add-hearthmirror-bridge` 正式实施。

## 3. `add-hearthmirror-bridge`（推荐先做，依赖 spike 已通过）

**类型**：业务能力 change，建立 `packages/hearthmirror/` 正式实现。

**范围**：

- `packages/hearthmirror/native/`：64 位 Rust crate，napi-rs 暴露 Mono 解析 API。
- 必须遵循 ADR 0001 的 Engineering Constraints：`RemotePtr(u32)` newtype、`#[napi] fn -> Result<T>`、`clippy::unwrap_used` CI 门禁。
- 实现 `IReflection` 的核心 12 方法：`GetCollection` / `GetMatchInfo` / `GetMedalInfo` / `GetDecks` / `GetBattleTag` / `GetAccountId` / `GetGameType` / `IsSpectating` / `IsGameOver` / `GetArenaState` / `GetArenaDeck` / `GetServerInfo`。
- `packages/hearthmirror/`：TypeScript 高层 API（`HearthMirror` 类、类型定义、错误处理）。
- `apps/desktop/src/main/ipc.ts`：`hearthmirror:*` 通道，渲染进程通过 `window.hdt.hearthmirror.*` 调用。

## 4. `add-card-database`

**类型**：业务能力 change。

**范围**：

- 新建 `packages/hearthdb`（pnpm workspace 包，依赖白名单仅 zod 之类的轻量验证库）。
- 引入下载脚本 `scripts/download-cards.ts`，从 HearthDb 项目获取最新 `Cards.json`。
- 实现 `CardDef` 类型 + 加载器 + 按 ID/名称/职业/法力值/稀有度/卡包查找。
- 卡组码（Hearthstone deck code）编解码算法。
- 暴露给 renderer 的 IPC：`window.hdt.cards.search(...)`。

## 5. `add-deck-management`

**类型**：业务能力 change。依赖：`add-card-database`。

**范围**：

- 新建 `packages/core/deck` + 引入 `better-sqlite3` 与 `zustand`。
- 卡组 CRUD（数据库 schema + migration）。
- 卡组合法性校验（30 张、同名 ≤ 2、传说 ≤ 1、职业限制）。
- 卡组导入（剪贴板文本、卡组码、JSON）/ 导出（卡组码、JSON）。
- Renderer 卡组选择器与编辑器（基于现有 `Dashboard.tsx` 与 `Decklist.tsx` 扩展）。

## 6. `add-hearthwatcher`

**类型**：业务能力 change。

**范围**：监听 Hearthstone Logs 目录，解析 PowerTaskList / LoadingScreen / Asset / Rank 等日志，构建实时 GameState。涉及的文件量大，建议先做 PowerTaskList 解析器与 GameState 模型，其余日志类型留给单独 change。

## 7. `add-overlay-window`

**类型**：业务能力 change。依赖：`add-hearthwatcher` 与 `add-deck-management`。

**范围**：独立透明 BrowserWindow + Hearthstone 窗口位置追踪 + 己方/对手卡组面板 + 回合计时器。OverlayView 从 `apps/desktop/renderer` 拆出到独立 BrowserWindow（当前 `/overlay` 路由仅做主窗口内的演示）。

---

> 每个 change 完成后用 `openspec validate <name> --strict` + `openspec status --change <name>` 验收，再用 `openspec archive <name>` 归档（归档后会迁出 `openspec/changes/`，并把 spec 沉淀到 `openspec/specs/`，作为下次 change 的 baseline）。
