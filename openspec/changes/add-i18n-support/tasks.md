## 1. Locale Runtime

- [x] 1.1 Add failing locale-resolution tests in `apps/desktop/src/renderer/tests/i18n-locale.test.ts` covering `expect(resolveAppLocale('system', 'zh-Hans-CN')).toBe('zh-CN')`, `expect(resolveAppLocale('system', 'fr-FR')).toBe('en-US')`, and `expect(resolveAppLocale('en-US', 'zh-CN')).toBe('en-US')`; run `pnpm --filter @hdt/desktop test i18n-locale` and expect failure because `../src/i18n/locale` does not exist.
- [x] 1.2 Implement `apps/desktop/src/renderer/src/i18n/locale.ts` exporting `type LanguagePreference = 'system' | 'en-US' | 'zh-CN'`, `type AppLocale = 'en-US' | 'zh-CN'`, `resolveAppLocale(preference, systemLanguage)`, and `toHearthstoneLocale(locale)`; run `pnpm --filter @hdt/desktop test i18n-locale` and expect the new locale tests to pass.
- [x] 1.3 Add failing translation lookup tests in `apps/desktop/src/renderer/tests/i18n-translate.test.ts` with sample dictionaries asserting `translate(messages, 'zh-CN', 'settings.title') === '设置'`, missing zh-CN key falls back to English, missing all locales returns the key, and interpolation replaces `{count}` with `2`; run `pnpm --filter @hdt/desktop test i18n-translate` and expect failure because `translate` is not exported.
- [x] 1.4 Implement `apps/desktop/src/renderer/src/i18n/messages.ts` and `apps/desktop/src/renderer/src/i18n/index.ts` exporting `translate`, `I18nProvider`, `useTranslation`, and `useLocale`; run `pnpm --filter @hdt/desktop test i18n-translate` and expect all translation tests to pass.
- [x] 1.5 Add `apps/desktop/src/renderer/src/i18n/i18n-store.ts` with Zustand-backed `languagePreference`, `setLanguagePreference`, `getActiveLocale`, and `localStorage` persistence behind storage key `hdt.languagePreference`; run `pnpm --filter @hdt/desktop typecheck` and expect no type errors in the new i18n modules.
- [x] 1.6 Commit runtime work with `git add apps/desktop/src/renderer/src/i18n apps/desktop/src/renderer/tests/i18n-*.test.ts && git commit -m "feat: add renderer i18n runtime"` and expect a Conventional Commit containing only runtime/test files.

## 2. Locale Resources and Settings

- [x] 2.1 Create failing dictionary parity test `apps/desktop/src/renderer/tests/i18n-messages.test.ts` importing `resources/locales/en-US.json` and `resources/locales/zh-CN.json`, flattening keys, and asserting `expect(zhKeys).toEqual(enKeys)`; run `pnpm --filter @hdt/desktop test i18n-messages` and expect failure because locale JSON files are missing.
- [x] 2.2 Add canonical locale files `resources/locales/en-US.json` and `resources/locales/zh-CN.json` covering app shell, sidebar, settings, deck tracker, deck selection dialog, opponent panel, card image states, and generic labels; run `pnpm --filter @hdt/desktop test i18n-messages` and expect key parity to pass.
- [x] 2.3 Add failing persistence tests in `apps/desktop/src/renderer/tests/i18n-store.test.ts` asserting `setLanguagePreference('zh-CN')` writes `localStorage['hdt.languagePreference'] === 'zh-CN'` and a fresh store initializes from that value; run `pnpm --filter @hdt/desktop test i18n-store` and expect failure until store hydration is complete.
- [x] 2.4 Finish `i18n-store.ts` hydration and invalid-value fallback to `system`; run `pnpm --filter @hdt/desktop test i18n-store` and expect persistence tests to pass.
- [x] 2.5 Add failing Settings render test in `apps/desktop/src/renderer/tests/Settings.i18n.test.tsx` rendering `Settings` inside `I18nProvider`, selecting `zh-CN`, and asserting `screen.getByText('设置')` and `screen.getByText('应用行为')` are visible; run `pnpm --filter @hdt/desktop test Settings.i18n` and expect failure because `Settings.tsx` still uses hard-coded strings.
- [x] 2.6 Update `apps/desktop/src/renderer/src/components/Settings.tsx` to use `useTranslation()` and add a language segmented/select control with options `system`, `en-US`, and `zh-CN`; run `pnpm --filter @hdt/desktop test Settings.i18n` and expect the localized Settings test to pass.
- [x] 2.7 Wrap the renderer root in `I18nProvider` in `apps/desktop/src/renderer/src/main.tsx` or `apps/desktop/src/renderer/src/App.tsx`; run `pnpm --filter @hdt/desktop test App` and expect existing App tests to still pass.
- [x] 2.8 Commit resource/settings work with `git add resources/locales apps/desktop/src/renderer/src/i18n apps/desktop/src/renderer/src/components/Settings.tsx apps/desktop/src/renderer/src/main.tsx apps/desktop/src/renderer/src/App.tsx apps/desktop/src/renderer/tests && git commit -m "feat: add language setting and locale resources"` and expect a Conventional Commit.

## 3. Card Locale Routing

- [x] 3.1 Add failing main-process card DB tests in `apps/desktop/src/main/cards.i18n.test.ts` using temp generated JSON files and asserting `ensureCardDb('zhCN').findById('EX1_277')?.name === '奥术智慧'` while missing locale falls back to `enUS`; run `pnpm --filter @hdt/desktop test cards.i18n` and expect failure because `ensureCardDb` does not accept a locale.
- [x] 3.2 Update `apps/desktop/src/main/cards.ts` to resolve `data/cards/generated/cards.all.${locale}.json`, cache `Promise<CardDb>` by Hearthstone locale, and fall back to `enUS` on missing localized data; run `pnpm --filter @hdt/desktop test cards.i18n` and expect card DB locale tests to pass.
- [x] 3.3 Add failing IPC/preload type tests or focused unit coverage asserting `window.hdt.cards.findById('EX1_277', 'zh-CN')`, `findByDbfId(dbfId, 'zh-CN')`, and `search(filter, 'zh-CN')` are accepted; run `pnpm --filter @hdt/desktop typecheck` and expect failure until IPC types are updated.
- [x] 3.4 Update `apps/desktop/src/main/ipc.ts` and `apps/desktop/src/preload/index.ts` so `cards:*` handlers accept app locale, map it through `toHearthstoneLocale`, and call `ensureCardDb(mappedLocale)`; run `pnpm --filter @hdt/desktop typecheck` and expect no preload/main type errors.
- [x] 3.5 Add failing renderer hook tests in `apps/desktop/src/renderer/tests/use-card-def.i18n.test.tsx` asserting `useCardDef('EX1_277')` calls `window.hdt.cards.findById('EX1_277', 'zh-CN')` when the active locale is Chinese; run `pnpm --filter @hdt/desktop test use-card-def.i18n` and expect failure because the hook does not pass locale.
- [x] 3.6 Update `apps/desktop/src/renderer/src/hooks/use-card-def.ts` and any bulk card lookup in `LiveDeckPanel.tsx` to include active locale in cache keys and IPC calls; run `pnpm --filter @hdt/desktop test use-card-def.i18n` and expect the hook test to pass.
- [x] 3.7 Add failing card-image locale test in `apps/desktop/src/renderer/tests/use-card-image-url.i18n.test.ts` asserting active `zh-CN` requests use `window.hdt.cardImages.get(cardId, 'zh-CN')`; run `pnpm --filter @hdt/desktop test use-card-image-url.i18n` and expect failure until locale is passed.
- [x] 3.8 Update `apps/desktop/src/preload/index.ts`, `apps/desktop/src/main/ipc.ts`, and `apps/desktop/src/renderer/src/hooks/use-card-image-url.ts` so card image requests pass app locale and primary `zhCN` still falls back to `enUS`; run `pnpm --filter @hdt/desktop test use-card-image-url.i18n card-image-cache` and expect all card image tests to pass.
- [ ] 3.9 Commit card locale routing with `git add apps/desktop/src/main apps/desktop/src/preload apps/desktop/src/renderer/src/hooks apps/desktop/src/renderer/src/components/LiveDeckPanel.tsx apps/desktop/src/renderer/tests && git commit -m "feat: route card data by app locale"` and expect a Conventional Commit.

## 4. Localize Existing UI Surfaces

- [x] 4.1 Add failing Sidebar/App render tests in `apps/desktop/src/renderer/tests/App.i18n.test.tsx` asserting Chinese locale shows `记牌器`, `统计`, `收藏`, and localized game-running status text; run `pnpm --filter @hdt/desktop test App.i18n` and expect failure because shell strings are hard-coded.
- [x] 4.2 Update `apps/desktop/src/renderer/src/App.tsx` and `apps/desktop/src/renderer/src/components/Sidebar.tsx` to use translation keys for status labels, mode switch labels, nav labels, and player fallback label; run `pnpm --filter @hdt/desktop test App.i18n App` and expect localized and existing App tests to pass.
- [x] 4.3 Add failing LiveDeckPanel i18n test in `apps/desktop/src/renderer/tests/LiveDeckPanel.i18n.test.tsx` asserting Chinese locale renders `等待对局开始`, `剩余卡牌`, and localized extras text while English locale renders `Waiting for match`; run `pnpm --filter @hdt/desktop test LiveDeckPanel.i18n` and expect failure until panel strings use i18n.
- [x] 4.4 Update `apps/desktop/src/renderer/src/components/LiveDeckPanel.tsx` to use `useTranslation()` for empty states, headers, extras, hand/opponent labels, error/live status, and unnamed deck fallback; run `pnpm --filter @hdt/desktop test LiveDeckPanel.i18n LiveDeckPanel` and expect all LiveDeckPanel tests to pass.
- [x] 4.5 Add failing DeckSelectDialog i18n test in `apps/desktop/src/renderer/tests/DeckSelectDialog.i18n.test.tsx` asserting English locale renders localized title, filter label, confirm, cancel, and empty-state strings; run `pnpm --filter @hdt/desktop test DeckSelectDialog.i18n` and expect failure until dialog strings use i18n.
- [x] 4.6 Update `apps/desktop/src/renderer/src/components/DeckSelectDialog.tsx` and related dialog tests to use translation keys for all user-visible text; run `pnpm --filter @hdt/desktop test DeckSelectDialog.i18n` and expect the dialog i18n test to pass.
- [x] 4.7 Update `apps/desktop/src/renderer/src/components/OpponentCardsPanel.tsx`, `Decklist.tsx`, and `CardImagePopover.tsx` user-visible labels to use translation keys where they are part of current desktop chrome; run `pnpm --filter @hdt/desktop test OpponentCardsPanel CardImagePopover` and expect existing tests to pass.
- [ ] 4.8 Commit UI localization with `git add apps/desktop/src/renderer/src apps/desktop/src/renderer/tests resources/locales && git commit -m "feat: localize desktop tracker UI"` and expect a Conventional Commit.

## 5. Validation

- [x] 5.1 Run `pnpm --filter @hdt/desktop test i18n-locale i18n-translate i18n-messages i18n-store Settings.i18n App.i18n LiveDeckPanel.i18n DeckSelectDialog.i18n use-card-def.i18n use-card-image-url.i18n cards.i18n` and expect all focused i18n tests to pass.
- [x] 5.2 Run `pnpm --filter @hdt/desktop test` and expect the full desktop Vitest suite to pass with no localized-string regressions.
- [x] 5.3 Run `pnpm --filter @hdt/desktop typecheck` and expect TypeScript strict mode to pass for renderer, preload, and main changes.
- [ ] 5.4 Run `pnpm lint` and expect no ESLint violations from new i18n modules, locale imports, or component string replacements.
- [x] 5.5 Run `openspec validate add-i18n-support --strict` and expect the change to validate without spec formatting or scenario errors.
- [ ] 5.6 Create final commit if validation changed files with `git add . && git commit -m "test: cover i18n support"` and expect either a new Conventional Commit or `nothing to commit, working tree clean`.
