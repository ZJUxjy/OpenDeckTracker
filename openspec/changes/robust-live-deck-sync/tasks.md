## 1. Deck Store Live Upsert

- [x] 1.1 In `apps/desktop/src/main/deck-store.test.ts`, add a test named `saveFromLive without liveDeckId creates app-managed deck` with assertions `expect(created.source).toBeNull()` or equivalent public metadata and `expect(created.liveDeckId).toBeNull()`; run `pnpm --filter @hdt/desktop exec vitest run src/main/deck-store.test.ts -t "without liveDeckId"` and expect it to fail before implementation.
- [x] 1.2 In `apps/desktop/src/main/deck-store.ts`, update `saveFromLive` so `source: 'hearthstone-live'` and `liveDeckId` are only set when `live.liveDeckId != null`; rerun the command from 1.1 and expect the targeted test to pass.
- [x] 1.3 In `apps/desktop/src/main/deck-store.test.ts`, add a test named `saveFromLive upserts unchanged liveDeckId without version bump` with test code asserting `second.id === first.id` and `second.version === first.version`; run `pnpm --filter @hdt/desktop exec vitest run src/main/deck-store.test.ts -t "upserts unchanged liveDeckId"` and expect it to pass.
- [x] 1.4 In `apps/desktop/src/main/deck-store.test.ts`, add a test named `saveFromLive bumps version when live card list changes` with test code asserting the same id and `second.version === first.version + 1`; run `pnpm --filter @hdt/desktop exec vitest run src/main/deck-store.test.ts -t "bumps version when live card list changes"` and expect it to pass.
- [x] 1.5 Commit store changes with `git add apps/desktop/src/main/deck-store.ts apps/desktop/src/main/deck-store.test.ts && git commit -m "fix(desktop): upsert live deck snapshots safely"`; expected output includes a new commit hash.

## 2. Main-Process Sync Host

- [x] 2.1 Create `apps/desktop/src/main/deck-sync-host.test.ts` with a test `syncFromLive returns unavailable without mutating store` using fake `getLiveDecks: async () => null` and test code `expect(result.source).toBe('unavailable')`; run `pnpm --filter @hdt/desktop exec vitest run src/main/deck-sync-host.test.ts -t "unavailable"` and expect it to fail because the module does not exist.
- [x] 2.2 Add `apps/desktop/src/main/deck-sync-host.ts` exporting `createDeckSyncHost({ syncOnce })` or equivalent around `createDeckSyncService`, returning `LiveDeckSyncResult`; rerun 2.1 and expect it to pass.
- [x] 2.3 Add a `deck-sync-host.test.ts` test `coalesces concurrent sync requests` with test code `await Promise.all([host.syncFromLive(), host.syncFromLive()]); expect(getLiveDecks).toHaveBeenCalledTimes(1)`; run `pnpm --filter @hdt/desktop exec vitest run src/main/deck-sync-host.test.ts -t "coalesces"` and expect it to pass.
- [x] 2.4 Add a `deck-sync-host.test.ts` test `reports not-ready before sync service is installed` with test code `expect(result.source).toBe('not-ready')`; run `pnpm --filter @hdt/desktop exec vitest run src/main/deck-sync-host.test.ts -t "not-ready"` and expect it to pass.
- [x] 2.5 Update `apps/desktop/src/main/ipc.ts` to instantiate the sync host before CardDb readiness, install the real sync service after `ensureCardDb()`, and keep startup sync best-effort; run `pnpm --filter @hdt/desktop typecheck` and expect no TypeScript errors from the edited files.
- [x] 2.6 Commit sync host changes with `git add apps/desktop/src/main/deck-sync-host.ts apps/desktop/src/main/deck-sync-host.test.ts apps/desktop/src/main/ipc.ts && git commit -m "feat(desktop): add live deck sync host"`; expected output includes a new commit hash.

## 3. IPC and Preload Surface

- [x] 3.1 In `apps/desktop/src/main/deck-ipc.test.ts`, add a test `syncFromLive resolves sync result` with test code `expect(await invoke('decks:sync-from-live')).toMatchObject({ source: 'live' })`; run `pnpm --filter @hdt/desktop exec vitest run src/main/deck-ipc.test.ts -t "syncFromLive resolves"` and expect it to fail before implementation.
- [x] 3.2 Update `apps/desktop/src/main/deck-ipc.ts` to accept `syncFromLive: () => Promise<LiveDeckSyncResult>` in `DeckIpcOptions`, register channel `decks:sync-from-live`, and remove/register it idempotently; rerun 3.1 and expect it to pass.
- [x] 3.3 Update `apps/desktop/src/preload/index.ts` so `window.hdt.decks.syncFromLive()` invokes `decks:sync-from-live`; add the missing test stub in `apps/desktop/src/renderer/tests/setup.ts`; run `pnpm --filter @hdt/desktop typecheck` and expect no `syncFromLive` type errors.
- [x] 3.4 Commit IPC/preload changes with `git add apps/desktop/src/main/deck-ipc.ts apps/desktop/src/main/deck-ipc.test.ts apps/desktop/src/preload/index.ts apps/desktop/src/renderer/tests/setup.ts && git commit -m "feat(desktop): expose live deck sync ipc"`; expected output includes a new commit hash.

## 4. Renderer Store and My Decks UI

- [x] 4.1 In `apps/desktop/src/renderer/tests/Decklist.saved.test.tsx`, add a test `syncs before rendering cached my decks` with test code `expect(window.hdt.decks.syncFromLive).toHaveBeenCalledBefore(window.hdt.decks.list as Mock)` or equivalent call-order assertion; run `pnpm --filter @hdt/desktop exec vitest run src/renderer/tests/Decklist.saved.test.tsx -t "syncs before"` and expect it to fail.
- [x] 4.2 Update `apps/desktop/src/renderer/src/stores/decks-store.ts` so `refresh({ sync: true })` or a new `syncAndRefresh()` first calls `syncFromLive()` then always calls `list()`; rerun 4.1 and expect it to pass.
- [x] 4.3 Update `apps/desktop/src/renderer/src/hooks/use-decks.ts`, `apps/desktop/src/renderer/src/components/SavedDecksTab.tsx`, and `apps/desktop/src/renderer/src/components/Decklist.tsx` to use the sync-aware refresh on My Decks route mount without changing mutation refresh behavior; run `pnpm --filter @hdt/desktop exec vitest run src/renderer/tests/Decklist.saved.test.tsx` and expect all tests in that file to pass.
- [x] 4.4 Update `apps/desktop/src/renderer/src/i18n/messages.ts`, `apps/desktop/src/renderer/src/components/DecksPage.tsx`, and tests `Decks.i18n.test.tsx` / `DecksPage.tabs.test.tsx` so English displays `My Decks` and Chinese displays `我的卡组`; run `pnpm --filter @hdt/desktop exec vitest run src/renderer/tests/Decks.i18n.test.tsx src/renderer/tests/DecksPage.tabs.test.tsx` and expect both files to pass.
- [x] 4.5 Commit renderer My Decks changes with `git add apps/desktop/src/renderer/src/stores/decks-store.ts apps/desktop/src/renderer/src/hooks/use-decks.ts apps/desktop/src/renderer/src/components/SavedDecksTab.tsx apps/desktop/src/renderer/src/components/Decklist.tsx apps/desktop/src/renderer/src/components/DecksPage.tsx apps/desktop/src/renderer/src/i18n/messages.ts apps/desktop/src/renderer/tests/Decklist.saved.test.tsx apps/desktop/src/renderer/tests/Decks.i18n.test.tsx apps/desktop/src/renderer/tests/DecksPage.tabs.test.tsx && git commit -m "feat(renderer): sync and rename my decks"`; expected output includes a new commit hash.

## 5. Collection and Deck Selection Triggers

- [x] 5.1 In `apps/desktop/src/renderer/tests/Collection.progress.test.tsx`, add a test `triggers live deck sync without blocking progress` with test code `expect(window.hdt.decks.syncFromLive).toHaveBeenCalledOnce()` and `expect(screen.getByText(/%|完成|Complete/)).toBeInTheDocument()`; run `pnpm --filter @hdt/desktop exec vitest run src/renderer/tests/Collection.progress.test.tsx -t "triggers live deck sync"` and expect it to fail before implementation.
- [x] 5.2 Update `apps/desktop/src/renderer/src/components/Collection.tsx` to fire `window.hdt.decks.syncFromLive()` on mount and ignore normal `ok: false` results while keeping collection progress rendering; rerun 5.1 and expect it to pass.
- [x] 5.3 In `apps/desktop/src/renderer/tests/DeckSelectDialog.saved.test.tsx`, add a test `syncs saved decks before showing choices` with test code `expect(window.hdt.decks.syncFromLive).toHaveBeenCalledOnce()` and assert cached saved decks still render when it resolves `{ ok: false }`; run `pnpm --filter @hdt/desktop exec vitest run src/renderer/tests/DeckSelectDialog.saved.test.tsx -t "syncs saved decks"` and expect it to fail before implementation.
- [x] 5.4 Update `apps/desktop/src/renderer/src/components/DeckSelectDialog.tsx` to call the sync-aware deck refresh before rendering final saved choices and to fall back to cached saved decks on sync failure; rerun 5.3 and expect it to pass.
- [x] 5.5 Commit trigger changes with `git add apps/desktop/src/renderer/src/components/Collection.tsx apps/desktop/src/renderer/src/components/DeckSelectDialog.tsx apps/desktop/src/renderer/tests/Collection.progress.test.tsx apps/desktop/src/renderer/tests/DeckSelectDialog.saved.test.tsx && git commit -m "feat(renderer): refresh decks on collection and selection"`; expected output includes a new commit hash.

## 6. Final Verification

- [x] 6.1 Run `pnpm --filter @hdt/desktop typecheck`; expected output exits 0 with no TypeScript errors.
- [x] 6.2 Run `pnpm --filter @hdt/desktop exec vitest run src/main/deck-store.test.ts src/main/deck-sync-service.test.ts src/main/deck-sync-host.test.ts src/main/deck-ipc.test.ts`; expected output exits 0. If `better-sqlite3.node` ABI mismatch appears, run `pnpm rebuild better-sqlite3` and repeat.
- [x] 6.3 Run `pnpm --filter @hdt/desktop exec vitest run src/renderer/tests/Decklist.saved.test.tsx src/renderer/tests/Decks.i18n.test.tsx src/renderer/tests/DecksPage.tabs.test.tsx src/renderer/tests/Collection.progress.test.tsx src/renderer/tests/DeckSelectDialog.saved.test.tsx`; expected output exits 0.
- [x] 6.4 Run `openspec status --change robust-live-deck-sync`; expected output shows all artifacts complete and tasks ready for apply tracking.
- [x] 6.5 Commit any final test-only fixes with `git commit -m "test(desktop): cover robust live deck sync"` if files changed; expected output includes a new commit hash or `nothing to commit` if no final changes were needed.
