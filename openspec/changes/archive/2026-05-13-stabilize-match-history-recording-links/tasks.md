## 1. Live Match Identity

- [x] 1.1 Add failing tests in `apps/desktop/src/main/match-identity.test.ts` for `createLiveMatchIdentityStore`: `beginLiveMatch(1000)` returns a Windows-safe fingerprint matching `/^match-v2-1000-\d+$/`, repeated `current()` calls return the same object, a second `beginLiveMatch(2000)` returns a different fingerprint, and `clear()` makes `current()` return `null`. Run `pnpm --filter @hdt/desktop exec vitest run src/main/match-identity.test.ts`; expected output: fails because `match-identity.ts` does not exist.
- [x] 1.2 Implement `apps/desktop/src/main/match-identity.ts` with `createLiveMatchIdentityStore()`, `beginLiveMatch(startedAt: number)`, `current()`, and `clear()`; fingerprint format MUST be filesystem-safe (`match-v2-${startedAt}-${sequence}`). Run `pnpm --filter @hdt/desktop exec vitest run src/main/match-identity.test.ts`; expected output: test file passes.
- [x] 1.3 Export or instantiate a main-process singleton in `apps/desktop/src/main/match-identity.ts` (`liveMatchIdentity`) so `hearthwatcher-host.ts`, `power-match-recorder.ts`, `deck-tracker.ts`, and `match-recording-recorder.ts` can share the same identity without circular imports. Run `pnpm --filter @hdt/desktop exec vitest run src/main/match-identity.test.ts`; expected output: test file passes.
- [ ] 1.4 Commit with message `feat(stats): add live match identity helper`.

## 2. Stable Match-History Writes

- [x] 2.1 Extend `apps/desktop/src/main/power-match-recorder.test.ts` with a failing test named `uses live match fingerprint when available`. Test setup: create recorder with `getMatchFingerprint: () => 'match-v2-1000-1'`, feed `create-game`, local `PLAYSTATE WON`, opponent `PLAYSTATE LOST`, and game complete; assert the recorded match has `fingerprint === 'match-v2-1000-1'`, `result === 'win'`, and `opponentClass` from snapshot. Run `pnpm --filter @hdt/desktop exec vitest run src/main/power-match-recorder.test.ts`; expected output: fails because recorder args do not accept `getMatchFingerprint`.
- [x] 2.2 Add a second failing test in `apps/desktop/src/main/power-match-recorder.test.ts` named `falls back to normalized fingerprint without live identity`; use `getMatchFingerprint: () => null`, complete a constructed game, and assert `record.mock.calls[0][0].fingerprint` starts with `v1|` or otherwise equals `buildMatchFingerprint(recordedMatch)`. Run the same Vitest command; expected output: fails until fallback is wired.
- [x] 2.3 Update `apps/desktop/src/main/power-match-recorder.ts` to accept optional `getMatchFingerprint?: () => string | null`; after `normalizeCompletedMatch`, replace the normalized `fingerprint` with the live fingerprint when available. Run `pnpm --filter @hdt/desktop exec vitest run src/main/power-match-recorder.test.ts`; expected output: power recorder tests pass.
- [x] 2.4 Extend `apps/desktop/src/main/deck-tracker.test.ts` with a failing test named `records match-ended with live fingerprint override`: mock the live identity helper to return `match-v2-1000-1`, emit a `match-ended` event with `completedMatch.fingerprint` set to a legacy value, and assert `recordCompletedMatch` receives `fingerprint: 'match-v2-1000-1'` while preserving other fields. Run `pnpm --filter @hdt/desktop exec vitest run src/main/deck-tracker.test.ts`; expected output: fails until the host stamps the identity.
- [x] 2.5 Update `apps/desktop/src/main/deck-tracker.ts` so the `match-ended` handler stamps `event.completedMatch` with `liveMatchIdentity.current()?.fingerprint` before calling `recordCompletedMatch`; do not mutate the event object sent to renderer unless tests require consistency. Run `pnpm --filter @hdt/desktop exec vitest run src/main/deck-tracker.test.ts`; expected output: deck tracker main tests pass.
- [x] 2.6 Update `apps/desktop/src/main/hearthwatcher-host.ts` so live `create-game` calls `liveMatchIdentity.beginLiveMatch(Date.now())` before dispatching to match/stat/recording recorders, and live game completion clears the identity only after all live recorders have handled the completion event. Add or update `apps/desktop/src/main/hearthwatcher-host.test.ts` to assert begin is called before `matchRecorder.handleEvent`. Run `pnpm --filter @hdt/desktop exec vitest run src/main/hearthwatcher-host.test.ts`; expected output: host tests pass.
- [x] 2.7 Add a regression test in `apps/desktop/src/main/match-history-store.test.ts` named `merges deck-tracker unknown and power result with shared live fingerprint`: record two `NormalizedCompletedMatch` objects with the same `fingerprint`, first `unknown`/null class, then `win`/`MAGE`; assert one row with enriched result/class. Run `pnpm --filter @hdt/desktop exec vitest run src/main/match-history-store.test.ts`; expected output: passes once local `better-sqlite3` ABI is usable.
- [ ] 2.8 Run `pnpm --filter @hdt/desktop exec vitest run src/main/power-match-recorder.test.ts src/main/deck-tracker.test.ts src/main/hearthwatcher-host.test.ts`; expected output: all listed tests pass. Commit with message `fix(stats): share live fingerprint across match recorders`.

## 3. Recording Fingerprint Metadata

- [x] 3.1 Extend `packages/core/src/recordings/match-recording.test.ts` with failing assertions that `createEmptyMatchRecording({ recordingId: 'rec-1', startedAt: 1000, matchFingerprint: 'match-v2-1000-1' })` stores `metadata.matchFingerprint`, and `buildMatchRecordingSummary(recording).matchFingerprint` equals the same value. Run `pnpm --filter @hdt/core test -- src/recordings/match-recording.test.ts`; expected output: fails until types and builder support the field.
- [x] 3.2 Update `packages/core/src/recordings/match-recording.ts` types and builders: add optional `matchFingerprint?: string` to `MatchRecordingMetadata` and `MatchRecordingSummary`, and accept optional `matchFingerprint` in `createEmptyMatchRecording`. Run `pnpm --filter @hdt/core test -- src/recordings/match-recording.test.ts`; expected output: core recording tests pass.
- [x] 3.3 Extend `apps/desktop/src/main/match-recording-recorder.test.ts` with a failing test named `stores live match fingerprint on recording summary`: create recorder with `getMatchFingerprint: () => 'match-v2-1000-1'`, feed `create-game` and game completion, and assert `store.writeRecording` receives a completed recording whose `metadata.matchFingerprint` and `finalSummary.matchFingerprint` equal `match-v2-1000-1`. Run `pnpm --filter @hdt/desktop exec vitest run src/main/match-recording-recorder.test.ts`; expected output: fails until recorder accepts the callback.
- [x] 3.4 Update `apps/desktop/src/main/match-recording-recorder.ts` to accept optional `getMatchFingerprint?: () => string | null`; pass the fingerprint into `createEmptyMatchRecording` at start and refresh metadata before finalization if it becomes available later. Run `pnpm --filter @hdt/desktop exec vitest run src/main/match-recording-recorder.test.ts`; expected output: tests pass.
- [x] 3.5 Update `apps/desktop/src/main/hearthwatcher-host.ts` recorder construction so `createPowerMatchRecorder` and `createMatchRecordingRecorder` both receive `getMatchFingerprint: () => liveMatchIdentity.current()?.fingerprint ?? null`. Run `pnpm --filter @hdt/desktop exec vitest run src/main/hearthwatcher-host.test.ts src/main/power-match-recorder.test.ts src/main/match-recording-recorder.test.ts`; expected output: all listed tests pass.
- [ ] 3.6 Commit with message `feat(recordings): persist match fingerprint on recordings`.

## 4. Recording Store and IPC Lookup

- [x] 4.1 Extend `apps/desktop/src/main/match-recording-store.test.ts` with a failing test named `loads recording detail by match fingerprint`: write a completed recording with `recordingId: 'rec-1'` and `metadata.matchFingerprint: 'match-v2-1000-1'`; assert `store.loadRecording('match-v2-1000-1')?.recordingId === 'rec-1'`. Run `pnpm --filter @hdt/desktop exec vitest run src/main/match-recording-store.test.ts`; expected output: fails until lookup scans/indexes by fingerprint.
- [x] 4.2 Add a second failing test in `apps/desktop/src/main/match-recording-store.test.ts` named `does not load by endedAt-only key`: write a completed recording with `endedAt: 5000` and no `matchFingerprint`; assert `store.loadRecording('match-v2-1000-1') === null`. Run the same Vitest command; expected output: fails if any endedAt fallback exists.
- [x] 4.3 Update `apps/desktop/src/main/match-recording-store.ts` so `loadRecording(idOrFingerprint)` first tries an exact recording directory, then scans completed/incomplete recording files for `metadata.matchFingerprint === idOrFingerprint`; do not match by `endedAt`. Run `pnpm --filter @hdt/desktop exec vitest run src/main/match-recording-store.test.ts`; expected output: store tests pass.
- [x] 4.4 Extend `apps/desktop/src/main/match-recordings-ipc.test.ts` with a failing test named `recordings:get accepts match fingerprint`: mock store `loadRecording` and assert the handler passes through `'match-v2-1000-1'`. Run `pnpm --filter @hdt/desktop exec vitest run src/main/match-recordings-ipc.test.ts`; expected output: should pass if IPC is already pass-through, otherwise fix handler wiring.
- [ ] 4.5 Run `pnpm --filter @hdt/core test -- src/recordings` and `pnpm --filter @hdt/desktop exec vitest run src/main/match-recording-store.test.ts src/main/match-recordings-ipc.test.ts`; expected output: all listed tests pass. Commit with message `fix(recordings): resolve details by match fingerprint`.

## 5. Stats Renderer Fingerprint Drill-In

- [x] 5.1 Extend `apps/desktop/src/renderer/tests/Stats.test.tsx` with a failing test named `opens recording viewer by match fingerprint`: mock recent match `fingerprint: 'match-v2-1000-1'`, mock `recordings.list()` returning `{ recordingId: 'rec-1', matchFingerprint: 'match-v2-1000-1', endedAt: null, ... }`, click `view-recording-<id>`, and assert `window.hdt.recordings.get` is called with `'match-v2-1000-1'`. Run `pnpm --filter @hdt/desktop exec vitest run src/renderer/tests/Stats.test.tsx`; expected output: fails because Stats currently maps by `endedAt`.
- [x] 5.2 Add a second failing test in `apps/desktop/src/renderer/tests/Stats.test.tsx` named `does not enable recording action from endedAt-only recording`: mock a recent match and a recording summary with the same `endedAt` but no `matchFingerprint`; assert the row button is disabled and `recordings.get` is not called. Run the same Vitest command; expected output: fails until endedAt map is removed.
- [x] 5.3 Update `apps/desktop/src/renderer/src/components/Stats.tsx`: replace `recordingByEndedAt` with `recordingByFingerprint`, built only from summaries carrying `matchFingerprint`; set viewer id to `match.fingerprint` when the fingerprint exists in the map. Run `pnpm --filter @hdt/desktop exec vitest run src/renderer/tests/Stats.test.tsx`; expected output: Stats tests pass.
- [x] 5.4 If TypeScript requires it, update exported `MatchRecordingSummary` usage in preload/renderer tests to include optional `matchFingerprint`; run `pnpm --filter @hdt/desktop typecheck`; expected output: TypeScript exits 0.
- [ ] 5.5 Commit with message `fix(renderer): open stats recordings by fingerprint`.

## 6. Verification

- [x] 6.1 Run `pnpm --filter @hdt/core test -- src/stats src/recordings`; expected output: all listed core tests pass.
- [x] 6.2 Run `pnpm --filter @hdt/desktop exec vitest run src/main/match-identity.test.ts src/main/power-match-recorder.test.ts src/main/deck-tracker.test.ts src/main/hearthwatcher-host.test.ts src/main/match-recording-recorder.test.ts src/main/match-recording-store.test.ts src/main/match-recordings-ipc.test.ts src/renderer/tests/Stats.test.tsx src/renderer/tests/MatchRecordingViewer.test.tsx`; expected output: all listed desktop tests pass. If `better-sqlite3` reports ABI mismatch, run `pnpm --filter @hdt/desktop rebuild:node` after closing processes that hold `better_sqlite3.node`, then rerun.
- [x] 6.3 Run `pnpm --filter @hdt/desktop typecheck`; expected output: TypeScript exits 0.
- [x] 6.4 Run `openspec validate stabilize-match-history-recording-links --strict`; expected output: change is valid.
- [ ] 6.5 Manual smoke with a live match: start app, begin and finish one constructed game, verify `stats.sqlite` has one row for the game after both completion sources run, navigate to Stats, and confirm `View recording` opens the matching recording. Commit final verified work with message `fix(stats): stabilize match history recording links`.
