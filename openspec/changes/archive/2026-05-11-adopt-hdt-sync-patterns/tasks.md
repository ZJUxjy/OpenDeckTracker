## 1. Match-start sync trigger

- [x] 1.1 Create `apps/desktop/src/main/match-start-sync-trigger.test.ts` with tests `triggers sync on IDLE -> PRE_MATCH`, `does not re-trigger within debounce window`, `re-triggers after debounce window elapses`, and `ignores PRE_MATCH following non-IDLE phases`; use a fake `onPhase` subscriber, a fake `now()`, and a Vitest spy for `syncFromLive`. Run `pnpm --filter @hdt/desktop exec vitest run src/main/match-start-sync-trigger.test.ts` and expect failures because the module does not exist.
- [x] 1.2 Add `apps/desktop/src/main/match-start-sync-trigger.ts` exporting `createMatchStartSyncTrigger({ onPhase, syncFromLive, now, minIntervalMs = 5000 })` that returns a `dispose()` function. The factory subscribes to phase events, tracks the previous phase, and calls `syncFromLive()` only on the `IDLE → PRE_MATCH` transition when `now() - lastTriggeredAt >= minIntervalMs`. Rerun 1.1 and expect all four tests to pass.
- [x] 1.3 Update `apps/desktop/src/main/ipc.ts` to import `createMatchStartSyncTrigger` and `onDeckTrackerPhase`, and to instantiate the trigger right after `deckSyncHost.setService(deckSync)` with `onPhase: onDeckTrackerPhase`, `syncFromLive: () => deckSyncHost.syncFromLive()`, and `now: Date.now`. Run `pnpm --filter @hdt/desktop typecheck` and expect exit 0.
- [x] 1.4 Commit with `git add apps/desktop/src/main/match-start-sync-trigger.ts apps/desktop/src/main/match-start-sync-trigger.test.ts apps/desktop/src/main/ipc.ts && git commit -m "feat(desktop): trigger live deck sync on match start"`; expected output includes a new commit hash.

## 2. Content-fingerprint reattach in saveFromLive

- [x] 2.1 In `apps/desktop/src/main/deck-store.test.ts`, add a test `saveFromLive reattaches new liveDeckId by content fingerprint when exactly one live-synced row matches` that (a) creates a live-synced row with `liveDeckId: 100` and a known card list, (b) calls `saveFromLive` with `liveDeckId: 200` and the same `(class, format, cards)`, then asserts `second.id === first.id`, `findByLiveDeckId(200) !== null`, `findByLiveDeckId(100) === null`, and `second.version === first.version` (no version bump on adopt). Run `pnpm --filter @hdt/desktop exec vitest run src/main/deck-store.test.ts -t "reattaches new liveDeckId"` and expect failure.
- [x] 2.2 Add a test `saveFromLive does NOT reattach when more than one live-synced row matches the fingerprint`: create two live-synced rows with the same `(class, format, cards)` but different `liveDeckId`s, then call `saveFromLive` with a third `liveDeckId` and the same content; assert `findByLiveDeckId(third) !== null` and the two original rows still resolve via their original ids. Run the same vitest command with the new test name and expect failure.
- [x] 2.3 Add a test `saveFromLive does NOT reattach against app-managed rows`: create an app-managed deck via `create({...})` with no live id and a known card list, then call `saveFromLive` with `liveDeckId: 500` and the same content; assert a new live-synced row exists (`findByLiveDeckId(500) !== null`) and the app-managed row's id is unchanged. Run the same vitest command with the new test name and expect failure.
- [x] 2.4 In `apps/desktop/src/main/deck-store.ts`, add a private helper `findLiveSyncedDecksByFingerprint(classCode, format, cardListHash): DeckDetail[]` that scans `decks` rows with `source = 'hearthstone-live'` and returns those whose current `card_list_hash` matches. In `saveFromLive`, when `hasLiveId` is true and `findByLiveDeckIdInternal(live.liveDeckId)` returns `null`, call the new helper; if it returns exactly one match, update that row's `liveDeckId` to `live.liveDeckId` (and apply the existing `updateLiveSyncedDeck` content/name refresh, treating identical cards as a no-op for version bumping). Otherwise fall through to `insertDeck(...)` with `{ source: 'hearthstone-live', liveDeckId }`. Rerun all three test names from 2.1–2.3 and expect them to pass.
- [x] 2.5 Run `pnpm --filter @hdt/desktop exec vitest run src/main/deck-store.test.ts`; expect exit 0 (the entire file still passes, including the prior `without liveDeckId`, `upserts unchanged liveDeckId`, and `bumps version when live card list changes` tests).
- [x] 2.6 Commit with `git add apps/desktop/src/main/deck-store.ts apps/desktop/src/main/deck-store.test.ts && git commit -m "feat(desktop): reattach live decks by card fingerprint"`; expected output includes a new commit hash.

## 3. Collection snapshot hash dedup

- [x] 3.1 In `apps/desktop/src/main/collection-snapshot-store.test.ts`, add a test `save() preserves lastUpdatedAt when card hash is unchanged` that (a) saves an initial collection with `lastUpdatedAt: T1`, (b) saves the same cards (possibly reordered) with `lastUpdatedAt: T2 = T1 + 60000`, and asserts the returned snapshot's `lastUpdatedAt === T1`. Run `pnpm --filter @hdt/desktop exec vitest run src/main/collection-snapshot-store.test.ts -t "preserves lastUpdatedAt"` and expect failure.
- [x] 3.2 Add a test `save() updates lastUpdatedAt when card count changes`: save `[{ dbfId: 1, count: 2, premium: 0 }]` at `T1`, then save `[{ dbfId: 1, count: 3, premium: 0 }]` at `T2`; assert returned `lastUpdatedAt === T2`. Run the same vitest command with the new test name and expect failure.
- [x] 3.3 Add a test `save() updates lastUpdatedAt when a new card is added`: save `[{ dbfId: 1, count: 2, premium: 0 }]` at `T1`, then save `[{ dbfId: 1, count: 2, premium: 0 }, { dbfId: 2, count: 1, premium: 1 }]` at `T2`; assert returned `lastUpdatedAt === T2`. Run the same vitest command with the new test name and expect failure.
- [x] 3.4 In `apps/desktop/src/main/collection-snapshot-store.ts`, add a private `computeCardsHash(cards)` returning a canonical string over sorted `(dbfId, premium, count)` triples joined with `|`. In `save()`, compute the new hash, read the stored `cardsHash` meta, and short-circuit when they match (return `{ cards: cards.map(...), lastUpdatedAt: storedLastUpdatedAt }` without writing). When they differ, write cards and update both `lastUpdatedAt` and `cardsHash` in the existing transaction. Add `cardsHash` to the `replaceCards` transaction or a new wrapper transaction so the meta write stays atomic with the cards rewrite. Rerun all three test names from 3.1–3.3 and expect them to pass.
- [x] 3.5 In `apps/desktop/src/main/collection-progress.test.ts`, add a test `collection-progress reports the snapshot's lastUpdatedAt unchanged when hash matches` that mocks `getCollection` to return the same cards on two calls separated by 60 seconds and asserts `response.lastUpdatedAt` is identical across the two responses. Run `pnpm --filter @hdt/desktop exec vitest run src/main/collection-progress.test.ts -t "lastUpdatedAt unchanged when hash matches"` and expect it to pass (no production change to `collection-progress.ts` should be required because it already consumes `save()`'s return value).
- [x] 3.6 Run `pnpm --filter @hdt/desktop exec vitest run src/main/collection-snapshot-store.test.ts src/main/collection-progress.test.ts`; expect exit 0.
- [x] 3.7 Commit with `git add apps/desktop/src/main/collection-snapshot-store.ts apps/desktop/src/main/collection-snapshot-store.test.ts apps/desktop/src/main/collection-progress.test.ts && git commit -m "feat(desktop): dedupe collection snapshot by content hash"`; expected output includes a new commit hash.

## 4. Final verification

- [x] 4.1 Run `pnpm --filter @hdt/desktop typecheck`; expect exit 0 with no TypeScript errors.
- [x] 4.2 Run `pnpm --filter @hdt/desktop exec vitest run src/main/deck-store.test.ts src/main/deck-sync-service.test.ts src/main/deck-sync-host.test.ts src/main/deck-ipc.test.ts src/main/match-start-sync-trigger.test.ts src/main/collection-snapshot-store.test.ts src/main/collection-progress.test.ts`; expect exit 0. If `better-sqlite3.node` ABI mismatch appears, run `pnpm rebuild better-sqlite3` first and repeat.
- [x] 4.3 Run `pnpm --filter @hdt/desktop exec vitest run src/renderer/tests/Decklist.saved.test.tsx src/renderer/tests/Decks.i18n.test.tsx src/renderer/tests/DecksPage.tabs.test.tsx src/renderer/tests/Collection.progress.test.tsx src/renderer/tests/DeckSelectDialog.saved.test.tsx`; expect exit 0 (no renderer-facing regression).
- [x] 4.4 Run `openspec status --change adopt-hdt-sync-patterns`; expected output shows all artifacts complete and tasks ready for apply tracking.
- [x] 4.5 Commit any final test-only fixes with `git commit -m "test(desktop): cover hdt sync pattern alignment"` if files changed; expected output includes a new commit hash or `nothing to commit` if no final changes were needed.
