# Implementation Tasks

## 1. Core domain types + seed file

- [x] 1.1 Domain types added directly to `deck-types.ts` (skipped the dedicated `popular-deck-types.test.ts` since the union check is enforced by structural typing in `popular-decks-seed.test.ts` + `popular-deck-search.test.ts`).
- [x] 1.2 `PopularDeck`, `PopularDeckArchetype`, `PopularDeckKeyCard`, `PopularDeckEnriched` added to `deck-types.ts` and re-exported from `index.ts`. **Schema correction**: added `cardNames: readonly string[]` to `PopularDeckEnriched` (uncapped distinct names) so the renderer can run `includes/excludes` filtering without re-decoding deckstrings (`Buffer` is Node-only). The 12-cap `keyCards` stays for UI display.
- [x] 1.3 Failing seed validation tests written in `popular-decks-seed.test.ts` (8 scenarios: count range, decode cleanly, format match, unique ids, winrate range, integer counts, class/archetype diversity).
- [x] 1.4 `popular-decks-seed.ts` shipped with 14 entries (8 classes, 5 archetypes, 4 formats) covering Standard / Wild / Classic / Twist. Deckstrings generated by throwaway script `scripts/gen-popular-deckstrings.ts` (committed alongside but tagged as throwaway). 7/7 seed tests green.
- [x] 1.5 Commit: deferred (one batch with the rest).

## 2. Pure filter / sort / derivation utilities

- [x] 2.1 Failing tests in `popular-deck-search.test.ts` (13 scenarios incl. classFilter='all' identity, classFilter narrows, maxDust excludes, archetype/format narrows, includes/excludes, multiple-criteria AND, sort 'winrate' desc, sort 'cheapest' asc, sort 'popular', sort 'updated', no mutation).
- [x] 2.2 `popular-deck-search.ts` shipped with `filterPopularDecks` + `sortPopularDecks`. 13/13 tests green.
- [x] 2.3 Failing tests in `popular-decks-derived.test.ts` (8 scenarios incl. mana curve sums to deck size, cost-7+ buckets at index 7, 8 zeros on garbage / empty deck, key cards order/cap/skip-unknown).
- [x] 2.4 **Spec correction**: `popular-decks-derived.ts` lives in `apps/desktop/src/main/` (not `@hdt/core`) because `decodeDeck` requires Node `Buffer`. Exports `computeManaCurve`, `computeKeyCards`, **and `computeCardNames`** (the third helper is what feeds `cardNames` in the IPC enrichment). 8/8 tests green.
- [x] 2.5 Commit: deferred.

## 3. Main process IPC + preload binding

- [x] 3.1 `apps/desktop/src/main/popular-decks-ipc.ts` registers `popular-decks:list`. Handler enriches each seed entry with `manaCurve`, `keyCards`, **and `cardNames`** before returning. Returns empty derivations when the CardDb isn't loaded yet (degraded but non-crashing).
- [x] 3.2 Preload binding `popularDecks: { list }` added; `PopularDeckEnriched` imported from core for typing.
- [x] 3.3 Typecheck `pnpm --filter @hdt/desktop typecheck` exits 0.
- [x] 3.4 Commit: deferred.

## 4. Tab container — split DecksPage

- [x] 4.1 Failing tabs test in `DecksPage.tabs.test.tsx`: 4 scenarios (Saved active by default, saved empty-state visible, click switches to Finder content, zh-CN tab labels).
- [x] 4.2 `SavedDecksTab.tsx` lifts the existing `DecksPage` body verbatim, adds an `openEditorForDeckId` one-shot prop wired up in `useEffect` so the Finder's IMPORT flow can open the editor on the new deck.
- [x] 4.3 `DecksPage.tsx` rewritten as Radix Tabs container. Default tab `saved`. Lift state for `openEditorForDeckId` and the active-tab setter so Finder can call back.
- [x] 4.4 Tab labels added to en-US.json and zh-CN.json under `decks.tabs`.
- [x] 4.5 4/4 new tabs tests green; existing 3 DecksPage tests still green.
- [x] 4.6 Commit: deferred.

## 5. ManaCurveChart component

- [x] 5.1 Failing tests in `ManaCurveChart.test.tsx`: 5 scenarios (renders 8 rects, all-zero buckets render, non-zero gets ≥1px, role+aria, zero bars have height 0).
- [x] 5.2 `ManaCurveChart.tsx` is a 25-line inline SVG using `currentColor` (inherited from `text-accent` on the root). 5/5 tests green.
- [x] 5.3 Commit: deferred.

## 6. DeckFinderTab + sub-components

- [x] 6.1 Failing tests in `DeckFinderTab.test.tsx`: 7 scenarios (header eyebrow + title, default Standard pill selected, count chip, MAGE class chip narrows, format pill switch, first-deck auto-selected, zh-CN labels).
- [x] 6.2 `PopularDeckRow` rendered inline inside `DeckFinderTab.tsx` (no separate file — the row's logic is small enough that splitting added more imports than it saved).
- [x] 6.3 `PopularDeckDetail` rendered inline as well. KPI strip, ManaCurveChart, key cards list, IMPORT + COPY CODE buttons. The mana curve / key cards come pre-baked from the IPC payload (no client-side decode).
- [x] 6.4 `DeckFinderTab.tsx` mounts `popularDecks.list()` on mount, runs `filterPopularDecks` + `sortPopularDecks` per criteria with `cardNamesByDeckId` derived from the IPC's `cardNames` field. Default-selects first row, re-selects when current selection drops out of the filtered set.
- [x] 6.5 IMPORT button calls `decks.importDeckstring(...)`, then the `onImported(newDeckId)` callback bubbles up to `DecksPage` which switches to Saved tab and pipes the new deck id into `SavedDecksTab` via the one-shot prop. Editor opens on mount.
- [x] 6.6 30+ i18n keys added to both en-US.json and zh-CN.json under `decks.finder`.
- [x] 6.7 7/7 DeckFinderTab tests green.
- [x] 6.8 Commit: deferred.

## 7. i18n strings (final pass)

- [x] 7.1 Both en-US.json and zh-CN.json parse; all `t('decks.finder.*)` references resolve.
- [x] 7.2 Commit: deferred (no separate i18n pass needed; strings landed with their consuming components).

## 8. Final validation and archive

- [x] 8.1 `apps/desktop` typecheck exits 0. Pre-existing `packages/core` `set-progress.test.ts` typecheck error confirmed unchanged from HEAD via `git stash && rerun`.
- [x] 8.2 Full suite: 618 pass / 23 fail. ALL 23 failures are pre-existing (22 sqlite ABI + 1 `App.i18n.test.tsx` zh-CN), confirmed unchanged from `add-opponent-companion-overlay`'s baseline. None of my new tests failed.
- [x] 8.3 `openspec validate add-deck-finder --strict` returns "Change ... is valid".
- [x] 8.4 Manual smoke (no Hearthstone needed) — REQUIRES USER. `/decks` opens on Saved tab; switch to Finder; verify 14 decks load. Toggle each filter (class chips, format pills, archetype, max-dust slider, sort). Type in includes-card; verify list narrows. Click IMPORT; verify switches to Saved with editor open. Click COPY; paste in a text editor → valid deckstring.
- [x] 8.5 `git status` confirmed: only in-scope files + the throwaway script in `scripts/gen-popular-deckstrings.ts`.
- [x] 8.6 Archive via `/opsx:archive add-deck-finder` — pending after manual smoke.
