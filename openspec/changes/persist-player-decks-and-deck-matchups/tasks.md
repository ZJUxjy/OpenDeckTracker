## 1. Match History Attribution

- [x] 1.1 Add failing migration tests in `apps/desktop/src/main/match-history-store.test.ts` for `savedDeckId` / `savedDeckVersion`: create a pre-migration `match_history` table without those columns, open `createMatchHistoryStore`, record `makeCompletedMatch({ fingerprint: 'saved-1', savedDeckId: 'deck-1', savedDeckVersion: 2 })`, and expect `getAllForFilter({ filter: 'all-time' })[0]` to include those fields. Run `pnpm --filter @hdt/desktop exec vitest run src/main/match-history-store.test.ts`; expected output: the new test fails because fields are not persisted.
- [x] 1.2 Implement additive `saved_deck_id TEXT` and `saved_deck_version INTEGER` migrations plus row mapping in `apps/desktop/src/main/match-history-store.ts`. Re-run `pnpm --filter @hdt/desktop exec vitest run src/main/match-history-store.test.ts`; expected output: match-history-store tests pass.
- [x] 1.3 Add failing enrichment tests in `apps/desktop/src/main/match-history-store.test.ts`: record an `unknown` result with null class values, then record the same fingerprint with `result: 'win'`, `opponentClass: 'MAGE'`, `playerClass: 'DRUID'`, and `savedDeckId: 'deck-1'`; expect one row enriched with the known values and no downgrade when the order is reversed. Run `pnpm --filter @hdt/desktop exec vitest run src/main/match-history-store.test.ts`; expected output: enrichment tests fail under `INSERT OR IGNORE`.
- [x] 1.4 Replace first-write-wins insert in `apps/desktop/src/main/match-history-store.ts` with an idempotent enrichment upsert keyed by `fingerprint`; prefer known result over `unknown` and non-null attribution over null. Re-run `pnpm --filter @hdt/desktop exec vitest run src/main/match-history-store.test.ts`; expected output: tests pass.
- [x] 1.5 Add failing tests in `apps/desktop/src/main/power-match-recorder.test.ts` where the snapshot has `opponentClass: 'MAGE'`, deck id/name, and saved-deck attribution, and the Power.log completion reports `WON`; expect the recorded match to include result plus class/deck attribution. Run `pnpm --filter @hdt/desktop exec vitest run src/main/power-match-recorder.test.ts`; expected output: test fails because class/attribution are missing.
- [x] 1.6 Update `apps/desktop/src/main/power-match-recorder.ts` to copy `snapshot.opponentClass`, deck identity, player class when available, and saved-deck attribution from the latest snapshot or completed match context. Re-run `pnpm --filter @hdt/desktop exec vitest run src/main/power-match-recorder.test.ts`; expected output: tests pass.
- [x] 1.7 Commit the match-history attribution slice with `git add apps/desktop/src/main/match-history-store.ts apps/desktop/src/main/match-history-store.test.ts apps/desktop/src/main/power-match-recorder.ts apps/desktop/src/main/power-match-recorder.test.ts && git commit -m "feat(desktop): persist saved deck match attribution"`; expected output: commit succeeds.

## 2. Saved-Deck Matchup Aggregation

- [x] 2.1 Add failing pure aggregation tests in `packages/core/src/stats/saved-deck-matchups.test.ts`: records for `deck-a` and `deck-b`, mixed `opponentClass`, mixed `result`, and mixed `formatType`; expect filtering by `savedDeckId`, winrate ignoring unknown results, and format filtering. Run `pnpm --filter @hdt/core exec vitest run src/stats/saved-deck-matchups.test.ts`; expected output: test file fails because module is missing.
- [x] 2.2 Implement `packages/core/src/stats/saved-deck-matchups.ts` with exported `SavedDeckMatchupStats` and `computeSavedDeckMatchups(records, savedDeckId, options)`, and export it from `packages/core/src/index.ts` or the existing stats barrel. Re-run `pnpm --filter @hdt/core exec vitest run src/stats/saved-deck-matchups.test.ts`; expected output: tests pass.
- [x] 2.3 Add failing IPC tests in `apps/desktop/src/main/stats-host.test.ts` for `stats:get-saved-deck-matchups`: stub a store with saved-deck records, invoke the handler with `deck-a`, `season`, and `{ formatFilter: 'standard' }`, and expect only matching buckets. Run `pnpm --filter @hdt/desktop exec vitest run src/main/stats-host.test.ts`; expected output: new test fails because handler is not registered.
- [x] 2.4 Add `getSavedDeckMatchups` handler in `apps/desktop/src/main/stats-host.ts` and expose it in `apps/desktop/src/preload/index.ts`. Re-run `pnpm --filter @hdt/desktop exec vitest run src/main/stats-host.test.ts`; expected output: tests pass.
- [x] 2.5 Add renderer tests in `apps/desktop/src/renderer/tests/Stats.test.tsx` or the existing Stats test file: mock `window.hdt.decks.list` and `window.hdt.stats.getSavedDeckMatchups`, select a saved deck, and expect a matchup row like `MAGE 1-1 50%`; expected initial command `pnpm --filter @hdt/desktop exec vitest run src/renderer/tests/Stats.test.tsx`; expected output: test fails because UI is not wired.
- [x] 2.6 Update `apps/desktop/src/renderer/src/components/Stats.tsx` to load saved decks, render a deterministic deck selector, call `getSavedDeckMatchups`, and show empty state when no deck/rows exist. Re-run `pnpm --filter @hdt/desktop exec vitest run src/renderer/tests/Stats.test.tsx`; expected output: tests pass.
- [x] 2.7 Commit the stats slice with `git add packages/core/src/stats apps/desktop/src/main/stats-host.ts apps/desktop/src/main/stats-host.test.ts apps/desktop/src/preload/index.ts apps/desktop/src/renderer/src/components/Stats.tsx apps/desktop/src/renderer/tests/Stats.test.tsx && git commit -m "feat(stats): add saved deck matchup aggregation"`; expected output: commit succeeds.

## 3. Player Profile Cache and Header Cleanup

- [x] 3.1 Add failing store tests in `apps/desktop/src/main/player-profile-store.test.ts`: create store in a temp path, `save({ battleTag: { name: 'P', fullBattleTag: 'P#1' }, accountId: { hi: 1n, lo: 2n } })`, reopen, and expect the same BattleTag plus `lastSeenAt`; also verify `save(null)` or failed reads do not erase existing identity. Run `pnpm --filter @hdt/desktop exec vitest run src/main/player-profile-store.test.ts`; expected output: module missing failure.
- [x] 3.2 Implement `apps/desktop/src/main/player-profile-store.ts` using SQLite or the chosen local-state DB with idempotent schema creation and serializable account id storage. Re-run `pnpm --filter @hdt/desktop exec vitest run src/main/player-profile-store.test.ts`; expected output: tests pass.
- [x] 3.3 Add failing IPC/preload tests in `apps/desktop/src/main/ipc.test.ts` or a new `player-profile-ipc.test.ts` verifying a `player-profile:get` handler returns cached identity and live HearthMirror BattleTag success refreshes the store. Run `pnpm --filter @hdt/desktop exec vitest run src/main/player-profile-ipc.test.ts`; expected output: handler missing failure.
- [x] 3.4 Register player-profile IPC in `apps/desktop/src/main/ipc.ts` and expose `window.hdt.playerProfile.get()` in `apps/desktop/src/preload/index.ts`. Re-run `pnpm --filter @hdt/desktop exec vitest run src/main/player-profile-ipc.test.ts`; expected output: tests pass.
- [x] 3.5 Add failing renderer tests in `apps/desktop/src/renderer/tests/header.test.tsx`: expect no Bell icon/button, no ChevronDown icon, player identity container is not a button, and cached BattleTag appears when `isAlive=false`. Run `pnpm --filter @hdt/desktop exec vitest run src/renderer/tests/header.test.tsx`; expected output: tests fail with current header.
- [x] 3.6 Update `apps/desktop/src/renderer/src/App.tsx` and `apps/desktop/src/renderer/src/hooks/use-hearthmirror-status.ts` to remove Bell/ChevronDown imports and markup, render display-only player identity, and use cached profile fallback. Re-run `pnpm --filter @hdt/desktop exec vitest run src/renderer/tests/header.test.tsx src/renderer/tests/use-hearthmirror-status.test.ts`; expected output: tests pass.
- [x] 3.7 Commit the profile/header slice with `git add apps/desktop/src/main/player-profile-store.ts apps/desktop/src/main/player-profile-store.test.ts apps/desktop/src/main/ipc.ts apps/desktop/src/preload/index.ts apps/desktop/src/renderer/src/App.tsx apps/desktop/src/renderer/src/hooks/use-hearthmirror-status.ts apps/desktop/src/renderer/tests/header.test.tsx apps/desktop/src/renderer/tests/use-hearthmirror-status.test.ts && git commit -m "feat(desktop): cache player identity for header"`; expected output: commit succeeds.

## 4. Collection Snapshot Cache

- [x] 4.1 Add failing tests in `apps/desktop/src/main/collection-snapshot-store.test.ts`: save collection cards `{ dbfId, count }`, reopen, and expect rows plus `lastUpdatedAt`; corrupt/missing store returns null instead of throwing. Run `pnpm --filter @hdt/desktop exec vitest run src/main/collection-snapshot-store.test.ts`; expected output: module missing failure.
- [x] 4.2 Implement `apps/desktop/src/main/collection-snapshot-store.ts` with compact `dbfId/count` persistence and timestamp. Re-run `pnpm --filter @hdt/desktop exec vitest run src/main/collection-snapshot-store.test.ts`; expected output: tests pass.
- [x] 4.3 Add failing tests in `apps/desktop/src/main/collection-progress.test.ts`: live collection success returns `source: 'live'` and refreshes cache; live null with cache returns cached owned counts and `source: 'cache'`; live null without cache returns zero counts and `source: 'empty'`. Run `pnpm --filter @hdt/desktop exec vitest run src/main/collection-progress.test.ts`; expected output: source/cache assertions fail.
- [x] 4.4 Update `apps/desktop/src/main/collection-progress.ts`, `apps/desktop/src/main/ipc.ts`, and `apps/desktop/src/preload/index.ts` to accept the snapshot store and return `source` / `lastUpdatedAt`. Re-run `pnpm --filter @hdt/desktop exec vitest run src/main/collection-progress.test.ts`; expected output: tests pass.
- [x] 4.5 Add failing renderer tests in `apps/desktop/src/renderer/tests/Collection.progress.test.tsx`: `source: cache` renders cached counts and stale banner; `source: empty` renders zero counts and launch-game banner. Run `pnpm --filter @hdt/desktop exec vitest run src/renderer/tests/Collection.progress.test.tsx`; expected output: cache-state tests fail.
- [x] 4.6 Update `apps/desktop/src/renderer/src/components/Collection.tsx` and `resources/locales/en-US.json` / `resources/locales/zh-CN.json` with cached/last-updated copy. Re-run `pnpm --filter @hdt/desktop exec vitest run src/renderer/tests/Collection.progress.test.tsx src/renderer/tests/i18n-messages.test.ts`; expected output: tests pass.
- [x] 4.7 Commit the collection slice with `git add apps/desktop/src/main/collection-snapshot-store.ts apps/desktop/src/main/collection-snapshot-store.test.ts apps/desktop/src/main/collection-progress.ts apps/desktop/src/main/collection-progress.test.ts apps/desktop/src/main/ipc.ts apps/desktop/src/preload/index.ts apps/desktop/src/renderer/src/components/Collection.tsx apps/desktop/src/renderer/tests/Collection.progress.test.tsx resources/locales/en-US.json resources/locales/zh-CN.json && git commit -m "feat(collection): show cached progress offline"`; expected output: commit succeeds.

## 5. Live Deck Snapshot Sync

- [x] 5.1 Add failing deck-store metadata tests in `apps/desktop/src/main/deck-store.test.ts`: create/update a live-synced deck with `source: 'hearthstone-live'` and `liveDeckId: 42`, verify manual decks lack this metadata and list/getById preserve it. Run `pnpm --filter @hdt/desktop exec vitest run src/main/deck-store.test.ts`; expected output: metadata assertions fail.
- [x] 5.2 Add additive metadata support to `apps/desktop/src/main/deck-store.ts` and core deck types in `packages/core/src/deck` as needed, preserving existing deck CRUD behavior. Re-run `pnpm --filter @hdt/desktop exec vitest run src/main/deck-store.test.ts`; expected output: tests pass.
- [x] 5.3 Add failing tests in `apps/desktop/src/main/deck-sync-service.test.ts`: given HearthMirror live decks, collectible lookup, and existing manual/live-synced records, expect non-destructive upsert, duplicate avoidance, and token deck skip. Run `pnpm --filter @hdt/desktop exec vitest run src/main/deck-sync-service.test.ts`; expected output: module missing failure.
- [x] 5.4 Implement `apps/desktop/src/main/deck-sync-service.ts` and wire it from `apps/desktop/src/main/ipc.ts` or startup after CardDb readiness so successful `hearthmirror.getDecks()` refreshes live-synced records. Re-run `pnpm --filter @hdt/desktop exec vitest run src/main/deck-sync-service.test.ts src/main/deck-store.test.ts`; expected output: tests pass.
- [x] 5.5 Add renderer regression test in `apps/desktop/src/renderer/tests/DecksPage.test.tsx` or `Decklist.saved.test.tsx`: with `window.hdt.decks.list` returning live-synced records and HearthMirror unavailable, Saved Decks still displays the deck rows. Run `pnpm --filter @hdt/desktop exec vitest run src/renderer/tests/Decklist.saved.test.tsx`; expected output: test passes after store/API shape updates.
- [x] 5.6 Commit the deck sync slice with `git add packages/core/src/deck apps/desktop/src/main/deck-store.ts apps/desktop/src/main/deck-store.test.ts apps/desktop/src/main/deck-sync-service.ts apps/desktop/src/main/deck-sync-service.test.ts apps/desktop/src/main/ipc.ts apps/desktop/src/renderer/tests/Decklist.saved.test.tsx && git commit -m "feat(decks): cache live Hearthstone decks locally"`; expected output: commit succeeds.

## 6. Final Verification

- [x] 6.1 Run focused unit tests: `pnpm --filter @hdt/core exec vitest run src/stats/saved-deck-matchups.test.ts` and `pnpm --filter @hdt/desktop exec vitest run src/main/match-history-store.test.ts src/main/power-match-recorder.test.ts src/main/stats-host.test.ts src/main/collection-progress.test.ts src/main/deck-sync-service.test.ts`; expected output: all listed tests pass.
- [x] 6.2 Run renderer tests: `pnpm --filter @hdt/desktop exec vitest run src/renderer/tests/header.test.tsx src/renderer/tests/use-hearthmirror-status.test.ts src/renderer/tests/Stats.test.tsx src/renderer/tests/Collection.progress.test.tsx src/renderer/tests/Decklist.saved.test.tsx src/renderer/tests/i18n-messages.test.ts`; expected output: all listed tests pass.
- [x] 6.3 Run typecheck: `pnpm --filter @hdt/core typecheck` and `pnpm --filter @hdt/desktop typecheck`; expected output: both commands exit 0.
- [x] 6.4 Manually smoke test with `pnpm --filter @hdt/desktop dev`: first with Hearthstone closed, confirm cached player/decks/collection display; then with Hearthstone open, confirm live values refresh. Expected output: dev server starts and manual observations match the specs.
- [x] 6.5 Commit any final test/typing fixes with `git commit -m "test(desktop): cover persisted offline state"` if needed; expected output: clean commit or no changes to commit.
