## 1. Renderer Test First

- [x] 1.1 In `apps/desktop/src/renderer/tests/LiveDeckPanel.test.tsx`, add a failing test named `renders friendly hand below remaining cards in hand order` that renders an active snapshot with `friendlyHand: ['COST10', 'COST1', 'COST5']`, mocks `window.hdt.cards.findById` to return names `Left Card`, `Middle Card`, `Right Card`, and asserts `screen.getByText(/current hand/i)` (or the zh-CN localized equivalent) appears after the remaining-card list header and the three hand card names appear top-to-bottom as Left/Middle/Right. Run `pnpm --filter @hdt/desktop test -- src/renderer/tests/LiveDeckPanel.test.tsx`; expected output before implementation: this new test fails because no friendly-hand section exists.
- [x] 1.2 In the same test file, add a failing test named `keeps remaining deck sort separate from hand order` using hand cards whose costs would sort differently from their hand order. Assert the hand section order follows `snapshot.friendlyHand` array order, while existing remaining deck rows still use the current cost/name ordering. Run `pnpm --filter @hdt/desktop test -- src/renderer/tests/LiveDeckPanel.test.tsx`; expected output before implementation: the new hand-order assertion fails.
- [x] 1.3 In `apps/desktop/src/renderer/tests/LiveDeckPanel.i18n.test.tsx` or `LiveDeckPanel.test.tsx`, add a failing zh-CN coverage case that checks the hand section label and empty hand message render through i18n when `friendlyHand` is empty. Run `pnpm --filter @hdt/desktop test -- src/renderer/tests/LiveDeckPanel.test.tsx src/renderer/tests/LiveDeckPanel.i18n.test.tsx`; expected output before implementation: missing localized hand label/empty text.

## 2. Snapshot Ordering Guard

- [x] 2.1 Inspect `packages/core/src/tracker/deck-tracker.ts` and confirm whether `buildSnapshot()` already maps `handState.friendlyHand` in `zonePosition` order. If it does not, add a failing test to `packages/core/src/tracker/deck-tracker.test.ts` named `orders friendlyHand by zonePosition` with hand state entries `{ cardId: 'B', zonePosition: 2 }`, `{ cardId: 'A', zonePosition: 1 }`, `{ cardId: 'C', zonePosition: 3 }`, expecting `snapshot.friendlyHand` to equal `['A', 'B', 'C']`. Run `pnpm --filter @hdt/core test -- src/tracker/deck-tracker.test.ts`; expected output before implementation: the new ordering test fails if sorting is missing.
- [x] 2.2 Implement the minimal core normalization in `packages/core/src/tracker/deck-tracker.ts` only if task 2.1 showed ordering is not guaranteed: sort `handState.friendlyHand` by `zonePosition` ascending before mapping to `cardId`, preserving existing filtering behavior. Run `pnpm --filter @hdt/core test -- src/tracker/deck-tracker.test.ts`; expected output: all tests in that file pass.

## 3. Renderer Implementation

- [x] 3.1 Update `apps/desktop/src/renderer/src/components/LiveDeckPanel.tsx` to derive hand card definitions for the union of remaining-card IDs and `snapshot.friendlyHand` IDs, reusing the existing `window.hdt.cards.findById` batching/cache pattern. Run `pnpm --filter @hdt/desktop test -- src/renderer/tests/LiveDeckPanel.test.tsx`; expected output: tests may still fail only on missing hand UI, not missing card names.
- [x] 3.2 Add a friendly-hand section below the remaining-card list in `LiveDeckPanel.tsx`. The section must render a localized header, a compact stable-height list of rows from `snapshot.friendlyHand` in array order, localized names with `cardId` fallback, optional mono position labels, and a localized empty message when the hand is empty. Run `pnpm --filter @hdt/desktop test -- src/renderer/tests/LiveDeckPanel.test.tsx`; expected output: tasks 1.1 and 1.2 pass.
- [x] 3.3 Add i18n keys in the renderer locale files under `apps/desktop/src/renderer/src/i18n/` for the hand section label and empty message in `en-US` and `zh-CN`. Run `pnpm --filter @hdt/desktop test -- src/renderer/tests/LiveDeckPanel.i18n.test.tsx src/renderer/tests/locales.global-effects.test.ts`; expected output: localized hand tests pass and locale parity remains green.

## 4. Visual Integration

- [x] 4.1 Verify `LiveDeckPanel.tsx` hand rows use Console theme token utilities only (`bg-*`, `border-*`, `text-*` mapped to tokens) and `font-mono` for numeric labels. Run `rg -n "#[0-9A-Fa-f]{3,8}|rgb\\(|oklch\\(" apps/desktop/src/renderer/src/components/LiveDeckPanel.tsx`; expected output: no hard-coded color literals introduced by this change.
- [x] 4.2 Ensure the hand section is rendered only in the player tracker path and does not appear in `apps/desktop/src/renderer/src/components/OpponentCardsPanel.tsx` or opponent overlay tests. Run `pnpm --filter @hdt/desktop test -- src/renderer/tests/OpponentCardsPanel.test.tsx src/renderer/tests/OpponentOverlayView.test.tsx`; expected output: existing opponent tests pass unchanged.

## 5. Verification and Commit

- [x] 5.1 Run targeted tests: `pnpm --filter @hdt/core test -- src/tracker/deck-tracker.test.ts` if core was changed, and `pnpm --filter @hdt/desktop test -- src/renderer/tests/LiveDeckPanel.test.tsx src/renderer/tests/LiveDeckPanel.i18n.test.tsx src/renderer/tests/OpponentCardsPanel.test.tsx`. Expected output: all targeted tests pass.
- [x] 5.2 Run `pnpm --filter @hdt/desktop typecheck`; expected output: TypeScript completes with no errors.
- [ ] 5.3 Run the desktop app with `pnpm --filter @hdt/desktop dev`, enter or replay an active match, and verify the player tracker shows remaining cards first and current hand below it in in-game order. Expected log/UI result: no renderer errors and the hand section updates after draw/play.
- [x] 5.4 Commit with `git commit -m "feat: show friendly hand in tracker"` after tests pass. Expected output: one commit containing the spec implementation and tests.
